import { StyleSheet } from "react-native";

export const titleStyles = StyleSheet.create({
    title: {
        fontWeight: "700",
        color: "purple",
        textAlign: "center",
        marginBottom: 20,
    }
})