import { StyleSheet } from "react-native";

export const titleStyles = StyleSheet.create({
    title: {
        fontWeight: "700",
        color: "purple",
        paddingBottom: 20,
        paddingLeft: 10,
    }
})